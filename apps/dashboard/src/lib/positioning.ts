// How PCC describes itself in one line.
//
// PCC_THESIS is the operator's sentence, used verbatim in description fields
// (meta, Open Graph, JSON-LD, the one-line definitions on about/index/llms.txt,
// /start, /docs, the context pack and /?mode=agent). PCC_BRAND is the short
// handle for titles and the landing hero label. Wherever the thesis appears,
// the public-beta status sentence sits beside it, so the thesis reads as the
// product's purpose rather than a live guarantee; the claims check enforces
// that (see public-claims.ts, claim "thesis").

export const PCC_NAME = "Physical Capability Cloud";

export const PCC_THESIS =
  "Turn abilities and inventions into trusted, economically callable capacity that other agents can immediately build on.";

export const PCC_BETA_STATUS = "Public beta: payments settle on a test network.";

export const PCC_BRAND = "the capability network";
