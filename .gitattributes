# The generated, byte-checked Phase-B browser kit MUST stay LF so `pnpm check:ir-kit`
# (esbuild emits LF) is byte-stable across platforms / contributors.
apps/dashboard/public/ui-kit/v1/pcc-ir-kit.js text eol=lf
