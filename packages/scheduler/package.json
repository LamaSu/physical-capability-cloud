{
  "name": "@pcc/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Workflow compiler + router — parses CWM and produces execution plans",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pcc/spec": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
