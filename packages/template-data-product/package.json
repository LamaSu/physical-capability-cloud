{
  "name": "@pcc/template-data-product",
  "version": "0.1.0",
  "private": true,
  "description": "Data-product publishing template for @pcc/orchestrator-sdk. Helps an organization expose a queryable dataset (Postgres / Snowflake / BigQuery / REST / GraphQL / MCP) as a paid PCC capability.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pcc/orchestrator-sdk": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
