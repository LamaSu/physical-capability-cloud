{
  "name": "@pcc/kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Shop Kernel — the boundary of trust for a physical site",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pcc/spec": "workspace:*",
    "fastify": "^4.28.0",
    "@fastify/cors": "^9.0.0",
    "better-sqlite3": "^11.0.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "tsx": "^4.11.0",
    "@types/better-sqlite3": "^7.6.0"
  }
}
