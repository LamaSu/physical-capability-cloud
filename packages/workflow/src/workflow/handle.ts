/**
 * WorkflowHandle factory helpers — mostly re-exports; the concrete
 * implementation lives inside engine.ts where it closes over the run state.
 */

export type { WorkflowHandle, StartWorkflowOptions } from './types.js';
