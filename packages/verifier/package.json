{
  "name": "@pcc/verifier",
  "version": "0.1.0",
  "private": true,
  "description": "Hybrid verifier service — open market (Tier 0/1) + curated guild (Tier 2/3)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pcc/spec": "workspace:*"
  },
  "devDependencies": {
    "@noir-lang/backend_barretenberg": "^0.36.0",
    "@noir-lang/noir_js": "1.0.0-beta.19",
    "vitest": "^1.6.0"
  }
}
