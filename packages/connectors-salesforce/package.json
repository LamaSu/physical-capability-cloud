{
  "name": "@pcc/connectors-salesforce",
  "version": "0.1.0",
  "private": true,
  "description": "Thin TS client for the connectors-runtime sidecar (dlt + salesforce source). Wraps POST /sources, POST /pipelines/{id}/run, GET /pipelines/{id}/status. NOTE: salesforce source is recognised by the runtime but its vendor SDK is wired in Wave 4; v0.1 returns 501.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
