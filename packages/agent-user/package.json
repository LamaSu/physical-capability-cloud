{
  "name": "@pcc/agent-user",
  "version": "0.1.0",
  "private": true,
  "description": "User Agent — holds wallet, submits jobs, negotiates with broker on behalf of user",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "default": "./dist/index.js" }
  },
  "scripts": { "build": "tsc", "test": "vitest run --passWithNoTests", "typecheck": "tsc --noEmit", "clean": "rm -rf dist" },
  "dependencies": {
    "@pcc/spec": "workspace:*",
    "@pcc/a2a": "workspace:*",
    "@pcc/agent-runtime": "workspace:*"
  },
  "devDependencies": { "vitest": "^1.6.0" }
}
