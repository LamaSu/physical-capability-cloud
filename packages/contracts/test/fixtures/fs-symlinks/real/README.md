Fixture for VNextDeployRecordTest: a symlinked directory (parent-link) and a dangling symlink (dangling.json).
forge must refuse both before reading or writing a deploy record. Do not replace the links with real files.
