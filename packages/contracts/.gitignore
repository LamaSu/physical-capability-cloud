# Foundry build artifacts
out/
cache/

# Foundry deployment artifacts (contain private key hashes, nonces, etc.)
broadcast/

# Dependencies (installed via forge install)
lib/

# Environment variables (secrets)
.env
.env.local
.env.*.local
