{
  "name": "@pcc/contract-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Declarative contract builder for manufacturing capabilities — schema-driven parameter discovery with pricing",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "default": "./dist/index.js" }
  },
  "scripts": { "build": "tsc", "test": "vitest run --passWithNoTests", "typecheck": "tsc --noEmit", "clean": "rm -rf dist" },
  "dependencies": {
    "@pcc/spec": "workspace:*"
  },
  "devDependencies": { "vitest": "^1.6.0" }
}
